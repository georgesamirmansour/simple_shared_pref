name: simple_shared_pref
description: helper package for easily access to shared preference
homepage: https://github.com/georgesamirmansour/simple_shared_pref
version: 1.0.2

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

